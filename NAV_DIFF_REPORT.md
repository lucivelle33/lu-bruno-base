# NAV_DIFF_REPORT — 全站导航统一工程

- 执刀:Bruno(Fable) | 日期:2026.07.07 | 范围:七文件,仅 `<nav class="bottom-nav">…</nav>` 区块

## 一、提货异常(重要)

包内 `chat.html` 与 `crosstalk.html` 为 **验收前旧版**(chat 缺三处已批修复:清空按钮拆除/章鱼头像/开房留言;crosstalk 缺定情曲种子)。已弃用包内版本,改用 Bruno 金库定版为基线施工。**下次打包请以最终验收版为准。**

## 二、逐页diff结论(与各自正确基线对比)

**v2 修订(2026.07.07):GPT复核抓到列数矛盾——nav为CSS grid固定列数,仅换HTML会导致7链接折行。已按最小例外工单补改,每页仅多动一行。**

|文件                 |变更内容                         |总diff行数|其余字节|
|-------------------|-----------------------------|-------|----|
|home.html          |nav追加山/聊 + `.bottom-nav`列数5→7|4      |未动  |
|observatory.html   |同上                           |4      |未动  |
|crosstalk.html(金库版)|同上                           |4      |未动  |
|reading.html       |同上                           |4      |未动  |
|gallery.html       |同上                           |4      |未动  |
|sanye.html         |nav追加聊 + 列数6→7               |3      |未动  |
|chat.html(金库版)     |零改动(本就7格7列)                  |0      |未动  |

例外范围核销:除nav区块外,每页仅改 `grid-template-columns: repeat(N,1fr)` 的N值,nav字号/图标/间距原样未动。

## 三、机检

- 七文件 JS `node --check` 全部通过
- 每文件 nav 链接 = 7,`aria-current="page"` 恰好 1 个且指向本页
- home.html 总链接 12(7 nav + 5 入口卡,符合预期)

## 四、390px 挤压评估(按工单只报告不动CSS)

七格,每格图标+1~2字。按现有 nav CSS(flex 均分)估算每格约 55px,汉字不换行,**可接受但无余量**;“星空”两字页签最宽,若真机显示局促,建议后续单独立项将其改单字”星”(文案改动需 Lu 批),本次未动。

## 五、上线指令

`/nav_unified/` 内七份全部上传仓库根目录,整批替换、一次提交。不要与旧版混traffic——七份是一套,拆开传会出现导航不一致窗口期。